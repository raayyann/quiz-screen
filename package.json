{
  "name": "quiz-screen",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "dev": "lerna run --parallel dev",
    "build": "lerna run --parallel build"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
